# Generated by Spider

# Generates unit tests to verify that the assets exists in assets directory
generate_tests: false

# Use this to remove vcs noise created by the `generated` comments in dart code
no_comments: true

# Exports all the generated file as the one library
export: true

# This allows you to import all the generated references with 1 single import!
use_part_of: true

# Generates a variable that contains a list of all asset values.
use_references_list: false

# Location where all the generated references will be stored
package: res

groups:
  - paths:
    - assets/images
    class_name: Images
    types: [ .png, .jpg, .jpeg, .webp ]

  - paths:
    - assets/images/mockup
    class_name: MockupImages
    types: [ .png, .jpg, .jpeg, .webp ]

  - path: assets/images/languages
    class_name: ImagesLocale
    types: [ .png, .jpg, .jpeg, .webp ]

  - path: assets/svgs/cryptos
    class_name: SvgCryptos
    types: [ .svg ]

  - path: assets/svgs/icons
    class_name: SvgIcons
    types: [ .svg ]

  - path: assets/svgs/
    class_name: Svgs
#    types: [ .svg ]
  - path: assets/rive/
    class_name: Rives

#  - path: assets/vectors
#    class_name: Svgs
#    package: res
#    types: [ .svg ]